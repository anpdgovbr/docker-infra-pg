# ============================================================================
# CONFIGURAÇÃO DOCKER POSTGRESQL - ANPD
# ============================================================================
# Template unificado para desenvolvimento local e GitOps/Portainer
# 
# 🏠 MODO LOCAL: Use docker-compose.yml com este arquivo
# 🚀 MODO GITOPS: Use docker-compose.gitops.yml com Portainer Environment Variables
#
# 📋 Aplicações definidas em: config/apps.conf
# ============================================================================

# ----------------------------------------------------------------------------
# 🗄️ POSTGRESQL - Configurações do Banco Principal
# ----------------------------------------------------------------------------
POSTGRES_USER=admin
POSTGRES_PASSWORD=SENHA_SUPER_SECRETA
POSTGRES_DB=postgres
POSTGRES_TIMEZONE=America/Sao_Paulo

# Opcionais para GitOps/Portainer (use defaults se não especificado):
# POSTGRES_PORT=5432
# POSTGRES_CONTAINER_NAME=anpd-postgres-dev
# POSTGRES_VOLUME_NAME=anpd_postgres_data

# ----------------------------------------------------------------------------
# 🌐 PGADMIN - Interface Web de Administração
# ----------------------------------------------------------------------------
PGADMIN_DEFAULT_EMAIL=admin@anpd.gov.br
PGADMIN_DEFAULT_PASSWORD=SENHA_PGADMIN

# Opcionais para GitOps/Portainer (use defaults se não especificado):
# PGADMIN_PORT=8085
# PGADMIN_CONTAINER_NAME=anpd-pgadmin-dev
# PGADMIN_VOLUME_NAME=anpd_pgadmin_data

# ----------------------------------------------------------------------------
# 🔐 APLICAÇÕES - Senhas dos Bancos das Apps
# ----------------------------------------------------------------------------
# Para desenvolvimento local, use APPS_CONFIG (formato completo):
APPS_CONFIG=backlog:backlog_dim_dev:backlog_user_db:senha_backlog,controladores:controladores_api_dev:controladores_user:senha_controladores

# Para GitOps/Portainer, use apenas as senhas (apps definidas em config/apps.conf):
BACKLOG_PASSWORD=senha_backlog_123
CONTROLADORES_PASSWORD=senha_controladores_456

# Opcionais para GitOps/Portainer:
# NETWORK_NAME=anpd_postgres_network
# INIT_CONTAINER_NAME=anpd-init-runner

# ============================================================================
# 📖 MODO DE USO:
# ============================================================================
#
# 🏠 DESENVOLVIMENTO LOCAL:
# 1. Copie este arquivo para .env
# 2. Ajuste as senhas
# 3. Execute: docker-compose up -d
#
# 🚀 GITOPS/PORTAINER:
# 1. Configure as variáveis de ambiente no Portainer Stack
# 2. Use docker-compose.gitops.yml
# 3. As aplicações são lidas automaticamente de config/apps.conf
#
# ============================================================================
