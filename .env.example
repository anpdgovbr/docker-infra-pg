# ============================================================================
# CONFIGURAÇÃO DA INFRAESTRUTURA POSTGRESQL MONO-APP
# ============================================================================
# Copie este arquivo para .env e configure as variáveis conforme sua aplicação

# CONFIGURAÇÕES DA APLICAÇÃO
# ============================================================================
# Nome da aplicação (usado para naming dos containers/volumes)
APP_NAME=minha-app

# CONFIGURAÇÕES DO POSTGRESQL ADMIN
# ============================================================================
# Usuário e senha do administrador do PostgreSQL
POSTGRES_USER=admin
POSTGRES_PASSWORD=senha_super_segura_admin

# Banco padrão do PostgreSQL (não mude, a menos que saiba o que está fazendo)
POSTGRES_DB=postgres

# Porta do PostgreSQL (padrão: 5432)
POSTGRES_PORT=5432

# CONFIGURAÇÕES DO BANCO DA APLICAÇÃO
# ============================================================================
# Nome do banco que sua aplicação vai usar
DB_NAME=minha_app_dev

# Usuário que sua aplicação vai usar para conectar
DB_USER=minha_app_user

# Senha do usuário da aplicação
DB_PASSWORD=senha_do_usuario_app

# ============================================================================
# EXEMPLO DE USO NA APLICAÇÃO
# ============================================================================
# Sua aplicação deve usar estas variáveis para conectar:
#
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@localhost:${POSTGRES_PORT}/${DB_NAME}
#
# Ou separadamente:
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=minha_app_dev
# DB_USER=minha_app_user
# DB_PASSWORD=senha_do_usuario_app
# ============================================================================
# 📖 MODO DE USO:
# ============================================================================
#
# 🏠 DESENVOLVIMENTO LOCAL:
# 1. Copie este arquivo para .env
# 2. Ajuste as senhas
# 3. Execute: docker-compose up -d
#
# 🚀 GITOPS/PORTAINER:
# 1. Configure as variáveis de ambiente no Portainer Stack
# 2. Use docker-compose.gitops.yml
# 3. As aplicações são lidas automaticamente de config/apps.conf
#
# ============================================================================
