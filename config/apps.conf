# Configuração de Aplicações - ANPD Docker PostgreSQL
# Este arquivo é versionado e define as aplicações suportadas pela infraestrutura
# As credenciais ficam no .env (local) ou secrets (Portainer/K8s)

# ============================================================================
# FORMATO: app_name:database_name:username
# As senhas são fornecidas via variáveis de ambiente ou secrets
# ============================================================================

# Aplicação: Sistema de Backlog de Demandas ANPD
backlog:backlog_dim_dev:backlog_user_db

# Aplicação: API de Controladores LGPD
controladores:controladores_api_dev:controladores_user

# ============================================================================
# ADICIONE NOVAS APLICAÇÕES AQUI
# Formato: nome_app:nome_banco:nome_usuario
# A senha será buscada na variável {NOME_APP}_PASSWORD (maiúsculo)
# Exemplo: Para 'nova_app', a senha virá de NOVA_APP_PASSWORD
# ============================================================================

# Aplicação: Sistema de Auditoria (exemplo comentado)
# auditoria:auditoria_dev:auditoria_user

# Aplicação: Sistema de Notificações (exemplo comentado)  
# notificacao:notificacao_dev:notificacao_user
